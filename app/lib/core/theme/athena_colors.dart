import 'package:flutter/material.dart';

class AthenaColors {
  AthenaColors._();

  // Fondo principal
  static const Color background = Color(0xFF081423);

  // Tarjetas
  static const Color card = Color(0xFF11253C);
  static const Color cardSecondary = Color(0xFF162D47);

  // Bordes
  static const Color border = Color(0xFF29445F);

  // Color corporativo
  static const Color primary = Color(0xFFD4AF37);

  // Estados
  static const Color success = Color(0xFF18C964);
  static const Color warning = Color(0xFFFFB800);
  static const Color danger = Color(0xFFFF4D4F);

  // Texto
  static const Color text = Colors.white;
  static const Color textSecondary = Color(0xFF9FB2C7);

  // Separadores
  static const Color divider = Color(0xFF20364D);
}