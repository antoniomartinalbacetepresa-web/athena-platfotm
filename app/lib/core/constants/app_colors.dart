import 'package:flutter/material.dart';

// ===============================================================
// ATHENA COLORS
// ===============================================================
//
// Toda la aplicación utilizará únicamente estos colores.
// Si algún día queremos cambiar la imagen corporativa,
// solo será necesario modificar este archivo.
// ===============================================================

class AppColors {
  AppColors._();

  // -------------------------------------------------
  // Marca
  // -------------------------------------------------

  static const Color primary = Color(0xFFD4AF37);

  static const Color secondary = Color(0xFFFFD54F);

  static const Color accent = Color(0xFF4FC3F7);

  // -------------------------------------------------
  // Fondos
  // -------------------------------------------------

  static const Color background = Color(0xFF0B0F18);

  static const Color surface = Color(0xFF141A24);

  static const Color card = Color(0xFF1B2330);

  // -------------------------------------------------
  // Texto
  // -------------------------------------------------

  static const Color textPrimary = Colors.white;

  static const Color textSecondary = Color(0xFFB0BEC5);

  static const Color textMuted = Color(0xFF78909C);

  // -------------------------------------------------
  // Estados
  // -------------------------------------------------

  static const Color success = Color(0xFF43A047);

  static const Color warning = Color(0xFFF9A825);

  static const Color danger = Color(0xFFE53935);

  static const Color info = Color(0xFF1E88E5);

  // -------------------------------------------------
  // Bordes
  // -------------------------------------------------

  static const Color border = Color(0xFF2D3748);

  static const Color divider = Color(0xFF263238);
}