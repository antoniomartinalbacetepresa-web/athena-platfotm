name: Validate ATHENA TYCHE

on:
  push:
    branches:
      - dev/real-market-backend
  pull_request:

permissions:
  contents: read

jobs:
  backend-tests:
    name: Backend tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install backend dependencies
        run: python -m pip install -r requirements.txt

      - name: Run backend tests
        run: python -m pytest -q --junitxml=pytest-results.xml

      - name: Upload backend test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-pytest-results
          path: backend/pytest-results.xml
          if-no-files-found: warn
          retention-days: 7

  flutter-validation:
    name: Flutter analyze, tests and release build
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: app
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Flutter version
        run: flutter --version

      - name: Install Flutter dependencies
        run: flutter pub get

      - name: Analyze
        run: |
          set -o pipefail
          flutter analyze 2>&1 | tee flutter-analyze.log

      - name: Test
        if: always()
        run: |
          set -o pipefail
          flutter test 2>&1 | tee flutter-test.log

      - name: Build release web bundle
        if: success()
        run: flutter build web --release

      - name: Upload Flutter validation logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: flutter-validation-logs
          path: |
            app/flutter-analyze.log
            app/flutter-test.log
            app/pubspec.lock
          if-no-files-found: warn
          retention-days: 7

      - name: Upload release web bundle
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: athena-web-release
          path: app/build/web
          if-no-files-found: error
          retention-days: 7

  release-validation-gate:
    name: Release validation gate
    if: always()
    needs:
      - backend-tests
      - flutter-validation
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require backend, Flutter and release-build validation
        env:
          BACKEND_RESULT: ${{ needs.backend-tests.result }}
          FLUTTER_RESULT: ${{ needs.flutter-validation.result }}
        run: |
          test "$BACKEND_RESULT" = "success"
          test "$FLUTTER_RESULT" = "success"
